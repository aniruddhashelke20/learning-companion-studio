services:
  mongo:
    image: mongo:7
    container_name: learnlog-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - learnlog_mongo_data:/data/db

volumes:
  learnlog_mongo_data:
