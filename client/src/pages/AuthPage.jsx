import { useState } from 'react'; import { useNavigate } from 'react-router-dom'; import api from '../api'; import { useAuth } from '../context/AuthContext';
export default function AuthPage({ register = false }) { const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const { authenticate } = useAuth(); const navigate = useNavigate(); const submit = async (e) => { e.preventDefault(); setError(''); try { const { data } = await api.post(`/auth/${register ? 'register' : 'login'}`, form); authenticate(data); navigate('/'); } catch (e) { setError(e.response?.data?.message || 'Unable to continue.'); } }; return <main className="grid min-h-screen place-items-center bg-slate-100 p-5"><section className="w-full max-w-md rounded-2xl bg-white p-8 shadow-sm"><h1 className="text-3xl font-bold text-ink">LearnLog</h1><p className="mb-6 mt-2 text-slate-600">{register ? 'Create your learning account.' : 'Welcome back to your learning space.'}</p>{error && <p className="mb-4 rounded bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<form onSubmit={submit} className="space-y-4">{register && <label className="block text-sm font-medium">Name<input required className="mt-1 w-full rounded border p-2" value={form.name} onChange={e => setForm({...form, name:e.target.value})}/></label>}<label className="block text-sm font-medium">Email<input required type="email" className="mt-1 w-full rounded border p-2" value={form.email} onChange={e => setForm({...form, email:e.target.value})}/></label><label className="block text-sm font-medium">Password<input required minLength="8" type="password" className="mt-1 w-full rounded border p-2" value={form.password} onChange={e => setForm({...form, password:e.target.value})}/></label><button className="w-full rounded bg-brand p-2.5 font-semibold text-white">{register ? 'Create account' : 'Sign in'}</button></form><p className="mt-5 text-sm text-slate-600">{register ? 'Already registered?' : 'New to LearnLog?'} <a className="font-semibold text-brand" href={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Create an account'}</a></p></section></main> }
