import { useEffect, useState } from 'react'; import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts'; import api from '../api'; import Layout from '../components/Layout';
export default function Analytics() { const [data, setData] = useState(null); useEffect(() => { api.get('/analytics/overview').then(r => setData(r.data)); }, []); if (!data) return <Layout><p>Loading analytics…</p></Layout>; const cards = [['Total learners', data.totalUsers], ['Average quiz score', `${data.averageScore}%`], ['Active in 24 hours', data.dailyActiveUsers]]; return <Layout><h1 className="text-3xl font-bold text-ink">Learning analytics</h1><p className="mt-2 text-slate-600">A Moodle-inspired view of activity across the platform.</p><div className="mt-6 grid gap-4 sm:grid-cols-3">{cards.map(([label, value]) => <div key={label} className="rounded-xl bg-white p-5 shadow-sm"><p className="text-sm text-slate-500">{label}</p><p className="mt-1 text-3xl font-bold text-ink">{value}</p></div>)}</div><div className="mt-6 rounded-xl bg-white p-5 shadow-sm"><h2 className="font-bold">Most common events</h2><div className="mt-4 h-72"><ResponsiveContainer><BarChart data={data.commonEvents}><XAxis dataKey="name" tick={{fontSize: 10}} interval={0}/><YAxis/><Tooltip/><Bar dataKey="count" fill="#2563eb" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="mt-6 rounded-xl bg-white p-5 shadow-sm"><h2 className="font-bold">Recent event log</h2><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b text-slate-500"><tr><th className="pb-2">Time</th><th className="pb-2">User</th><th className="pb-2">Component</th><th className="pb-2">Event</th><th className="pb-2">Context</th></tr></thead><tbody>{data.recentEvents.map(e => <tr className="border-b" key={e._id}><td className="py-3">{new Date(e.createdAt).toLocaleString()}</td><td>{e.userId?.name || 'Unknown'}</td><td>{e.component}</td><td>{e.eventName}</td><td>{e.eventContext}</td></tr>)}</tbody></table></div></div></Layout> }
