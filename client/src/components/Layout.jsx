import { Link, NavLink, useNavigate } from 'react-router-dom';
import { BookOpen, LayoutDashboard, BarChart3, LogOut } from 'lucide-react';
import { useAuth } from '../context/AuthContext';
export default function Layout({ children }) { const { user, logout } = useAuth(); const navigate = useNavigate(); const leave = () => { logout(); navigate('/login'); }; return <div className="min-h-screen"><header className="border-b bg-white"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5"><Link to="/" className="flex items-center gap-2 text-xl font-bold text-ink"><span className="rounded-lg bg-brand p-2 text-white"><BookOpen size={18}/></span>LearnLog</Link><nav className="flex items-center gap-4 text-sm"><NavLink to="/" end>Dashboard</NavLink>{user?.role === 'admin' && <NavLink to="/analytics" className="flex gap-1"><BarChart3 size={16}/>Analytics</NavLink>}<span className="hidden text-slate-500 sm:inline">{user?.name}</span><button onClick={leave} title="Sign out" className="text-slate-500"><LogOut size={18}/></button></nav></div></header><main className="mx-auto max-w-6xl px-5 py-8">{children}</main></div> }
